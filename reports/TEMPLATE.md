# Session report — `<session id>`

Fill every section. Write "none" rather than omitting a section. Do not edit reports from earlier sessions.

## 1. Acceptance criteria

One row per criterion in `scaffold-spec.md` for this session type. `evidence` is the exact command and the relevant output line, not a description.

| id | criterion | status | test file::name | evidence |
|---|---|---|---|---|
| SC-1 | | pass / fail / not attempted | | |

## 2. Decisions I made that the spec did not

Anything the spec left open and you chose. One line each: what, what you picked, why. Examples: page-size default, error message wording, how the UI renders the diff.

## 3. Ambiguities and questions

Anything you would have asked a human if one were available. Include what you assumed instead. This section is a primary result — be thorough, not defensive.

## 4. Things I wanted to change but did not

Any point where a scaffold table, trigger, role, `withActor`, `route`, or CI config seemed to need modification. What, and why you thought so. Empty means the scaffold fit.

## 5. Dependencies added

Every package added to any `package.json`, with a one-line reason. Include dev dependencies.

## 6. Tests I disabled, skipped, or weakened

Should be "none". If not, what and why.

## 7. Knowledge and context used

Which Knowledge notes you accessed and at what point. Which files you read from the repo before writing code.

## 8. Timeline

Phases in order with approximate minutes: reading, planning, migrations, server, UI, tests, debugging, PR. Note any phase you had to redo.

## 9. Governance report

Paste the output of `npm run governance-report` for this app.
